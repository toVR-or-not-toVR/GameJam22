using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CO2Fix : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        transform.eulerAngles = new Vector3(331.990021f, 82.8999863f, 4.43000031f);
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
